const { arraysum } = require('./function2');
const { divide } = require('./function1');

console.log('start of manager');
const arr = [1, 2, 3, 4, 5, 6];

console.log(arraysum(arr));

console.log(divide(10, 5));


console.log('end of manager');
